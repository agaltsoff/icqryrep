
&Вместо("ПолучитьЗапросПоПредставлению")
Функция амг_ПолучитьЗапросПоПредставлению(ТекстЗапроса, СоответствиеПараметров)
	
	Запрос = ПродолжитьВызов(ТекстЗапроса, СоответствиеПараметров);
	
	Если Запрос = Неопределено Тогда
	
		Запрос = амг_ОбщиеНаборыДанных.ПолучитьЗапросПоПредставлению(ТекстЗапроса, СоответствиеПараметров);
	
	КонецЕсли;
	
	Возврат Запрос;
	
КонецФункции
